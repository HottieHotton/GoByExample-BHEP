When looking up other examples to explain interfaces, I found a really great example. The interface is a USB Port. The devices used to plug into the USB Port is structs(like keyboard, mouse, etc).

This is a good way to understand interfaces and how they work.