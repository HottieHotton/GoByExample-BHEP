I did switch statements in Java and Python before, so this was pretty standard. But i hadn't had a chance to mess with the type command in any language really. Honestly, Switch Statements are glorified if/else statements. But are useful in many other instances.

Also used a printf statement before, which allows users to "format" strings with your variables.