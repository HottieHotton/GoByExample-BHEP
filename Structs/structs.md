Structs remind me of how I usually handle JSON objects within Python. Using dots to access fields, having key/value based pairs, and more. It also reminds me of objects like in C# and in Java.

I did find this line really cool:
> Go is a garbage collected language; you can safely return a pointer to a local variable - it will only be cleaned up by the garbage collector when there are no active references to it.

Especially with big projects, knowing this is helpful because you can ensure your code is being efficient.