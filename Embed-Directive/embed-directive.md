From Go By Example:
> //go:embed is a compiler directive that allows programs to include arbitrary files and folders in the Go binary at build time.

Utilizing embed.FS will utilize multiple files or even folders