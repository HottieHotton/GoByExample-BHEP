I started out a little confused about the SlicesIndex functions, but after reading deeper in the Generics page on GoByExample, I had a better understanding that we just did a slices.Index but in raw code(I utilized both for compaision in behavior, which is the same). We focused on the `any` type which allows for any value to be submitted. It can also be applied to slices and arrays.