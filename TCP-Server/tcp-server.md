This was really interesting, I actually had to run this differently than the example provided because I'm doing this on windows(at the moment), so I had to enable a feature, log into the server, and then run the command and it printed.

But it runs near the same as how a normal HTTP Server handles request/responses!